// One-file entry for the flat static bundle (Cloudflare Drop and any host
// that only serves root-level files). Bundles app.js + lib + vendors into a
// single self-contained bundle.js.
import '../site/public/app.js';
